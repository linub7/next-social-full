import SigninFromComponent from '@/components/auth/signin/form';

interface Props {}

const SigninPage = (props: Props) => {
  return (
    <div>
      <SigninFromComponent />
    </div>
  );
};

export default SigninPage;
