import SignupFromComponent from '@/components/auth/signup/form';

interface Props {}

const SignupPage = (props: Props) => {
  return (
    <div>
      <SignupFromComponent />
    </div>
  );
};

export default SignupPage;
